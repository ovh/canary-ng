---
global:
  scrape_interval: 1s
  scrape_timeout: 1s
  evaluation_interval: 1s

scrape_configs:
- job_name: canary-ng
  metrics_path: /metrics
  scheme: http
  static_configs:
  - targets:
    - canary-ng:8080
